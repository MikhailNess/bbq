<div class="buffer-bottom">
  <p><%= comment.body %></p>
  <p>
    <span>
      <% if comment.user.present? %>
        <%= render partial: "users/user", object: comment.user %>
      <% else %>
        <em><strong><%= comment.user_name %></strong></em>
      <% end %>
    </span>
    <em class="registration-label"><%= l comment.created_at, format: :short %></em>

     <div class="align-self-end ms-auto p-2 bd-highlight">
      <% if current_user_can_edit?(comment) %>
        <div class="buffer-top mt-0">
          <%= button_to t('app.delete'), event_comment_path(comment.event, comment), class: 'btn btn-danger text-sm',
                        method: :delete, data: { confirm: t(".destroy_confirm")} %>
        </div>
      <% end %>
    </div>
    </p>
</div>
